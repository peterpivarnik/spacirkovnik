<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Vyžaduje touchscreen — zámerne vylúči Android TV, wearables a iné zariadenia bez dotykovej obrazovky -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="true" tools:ignore="TouchscreenHardwareFeature" />
    <!-- GPS je nevyhnutné — celá špacírka je navigácia na reálne GPS body. Vylúči TV, Chromebooky a XR bez GPS. -->
    <uses-feature android:name="android.hardware.location.gps" android:required="true" />
    <!-- Kompas (magnetometer) — používa ho navigačná strelka, ale NIE je povinný: niektoré lacnejšie
         telefóny ho nemajú a tých nechceme vylúčiť. Appka beh bez kompasu zvládne (strelka sa len neotáča). -->
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />

    <application
        android:name=".SpacirkovnikApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Spacirkovnik">
        <meta-data
            android:name="com.mapbox.token"
            android:value="${MAPBOX_PUBLIC_TOKEN}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Spacirkovnik">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>